{
  "name": "jevtrends",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "check": "astro check",
    "build": "node scripts/sync-x.mjs && astro build",
    "sync:x": "node scripts/sync-x.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": {
    "astro": "7.3.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@types/node": "^22.20.4",
    "typescript": "6.0.3"
  }
}
